import 'package:flutter/material.dart';

class AppColors {
  static const Color black = Color(0xFFFFFFFF);
  static const Color black2 = Color(0xFFF0F0F0);
  static const Color black3 = Color(0xFFE0E0E0); // elevated surface (modals, lifted panels)
  static const Color yellow = Color(0xFFFFC107);   // primary actions + prices ONLY
  static const Color amber = Color(0xFFC9A227);    // secondary accent: borders, inactive highlights
  static const Color red = Color(0xFFE53935);       // urgent/destructive only
  static const Color white = Color(0xFFFFC107);     // primary text color
  static const Color grey = Color(0xFF9E9E9E);
}